server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA routing: let React Router handle all non-file paths
    location / {
        try_files $uri $uri/ /index.html;
    }
}
